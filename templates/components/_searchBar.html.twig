<div class="container mt-4 row justify-content-center">
        <div class="col-3">
                <label for="searchInput" class="form-label visually-hidden"></label>
                <input
                        type="text"
                        class="form-control"
                        id="searchInput"
                        data-action="search-preview#onSearchInputEvent"
                        value="{{ search }}"
                        placeholder="{{ 'app.twig.component.searchBar.search_text'|trans }}"
                >
        </div>
</div>
