<div class="container mt-4">
    {{ form_start(form,{'attr':{'class': 'row justify-content-center'}}) }}
    <div class="col-3">
        {{ form_row(form.q) }}
    </div>
    <div class="col-2">
        {{ form_row(form.submit) }}
    </div>
    {{ form_end(form) }}
</div>
